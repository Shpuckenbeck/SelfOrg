@model IEnumerable<SelfOrg.Models.Multiplier>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Lower)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Higher)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Mul)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lower)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Higher)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mul)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MultiplierId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MultiplierId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MultiplierId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
