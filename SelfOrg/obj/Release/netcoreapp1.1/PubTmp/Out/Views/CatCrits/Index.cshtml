@model IEnumerable<SelfOrg.Models.CatCrit>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.prio)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Criterion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.prio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.CatDescr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Criterion.description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CatCritId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CatCritId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CatCritId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
